[workspace]
resolver = "2"
members = [
    #"rsvp-cli", 
    "rsvp-format",
    #"rsvp-pipeline"
    ]

[workspace.dependencies]
bincode-next = {version = "3.1.1", default-features = false, features=["zero-copy", "derive"]}
thiserror = {version = "2.0.18", default-features = false}